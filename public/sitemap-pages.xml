import { execSync } from "child_process";
import fs from "fs";

const lastCommitDate = execSync("git log -1 --format=%cI").toString().trim();

const pages = [
  "https://www.rpovoadata.tech/",
  "https://www.rpovoadata.tech/about",
  "https://www.rpovoadata.tech/personal",
  "https://www.rpovoadata.tech/professional",
  "https://www.rpovoadata.tech/why-me",
  "https://www.rpovoadata.tech/side-projects",
  "https://www.rpovoadata.tech/contact",

];

const urls = pages.map(url => `
  <url>
    <loc>${url}</loc>
    <lastmod>${lastCommitDate}</lastmod>
  </url>
`).join("");

const sitemapPages = `<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  ${urls}
</urlset>`;

// 👉 AQUI MUDA O NOME
fs.writeFileSync("./dist/sitemap-pages.xml", sitemapPages);